# 테스트 가이드 (기능별 권장 아이템)

개발용 빌드에서 **F7(아이템 추가)** 로 아래 아이템들을 넣어 시나리오를 구성한다.
각 시나리오는 **준비 → 배치 → F8 → 기대결과 → F9** 순. `LogOutput.log` 의 GRID DUMP / `[artifact]`·`[tablet]`·`[base effects]` 줄로 확인한다.

> 이름이 불확실한 항목은 **로그 표시(`[특수:...]`, `criteria=...`, `[발동조건有]`)** 로 식별하면 된다.

---

## T1. 석판 발동 조건 + 다중재시작 (핵심 회귀 테스트)
- **준비**: 석판 **'정의'** 1개 + 레벨 상한 2 이상 아티팩트 5~6개 (예: 행운의 메달, 가시 부적, 간이 계약서, 압박 밴드 등 아무거나)
- **배치**: '정의' 석판을 가운데/아래쪽에 두고 아티팩트를 흩뿌림
- **기대**: F8 → '정의'를 **왼쪽(또는 오른쪽) 끝 열**로 옮기고 그 열에 아티팩트 채우는 추천, `점수 before→after` 에서 after 가 크게 상승. F9 적용 시 그대로 반영.
- 로그: 정의 석판 줄에 `[발동조건有]`.

## T2. 석판 회전 자동
- **준비**: 한 방향으로 +N 주는 **방향성 석판** (예: '미래', '입구', '고양', '기반')
- **기대**: 로그 `rotatable=True`, 석판 줄에 `↻현재→추천` 회전 추천. F9 시 실제로 석판 방향이 돌아감.
- **대조**: 회전 불가 석판은 로그 `rotatable=False`, 회전 안 바뀜.

## T3. 위치 기반 발동 제약 아티팩트
- **준비** (각 1개):
  - **검술 교본** / **마법 당근** → 맨 윗줄(TopInInventory)
  - **따뜻한 돌** → 안쪽(Inside)
  - **축사의 검집** / **용골 파편** → 바깥쪽(Outlined)
  - **다용도 벨트** → 맨 아래줄(BottomInInventory)
- **기대**: 각자 조건 만족 칸으로 배치. 조건 못 맞추는 칸이면 오버레이에 `Lv·비활성`.

## T4. 이웃 기반 발동 제약
- **준비**: `criteria=CharmActivateCriteria_BothSidesAreEmpty`(양쪽 빈칸) 또는 `_BothSideCharm`(양쪽 부적) 아티팩트
  → 정확한 이름은 인게임 설명/로그 `criteria=` 로 식별
- **기대**: 양쪽이 비거나(또는 양옆에 부적) 조건이 충족되는 칸에 배치. 다른 아티팩트를 옆에 모으거나 비우는 추천.

## T5. 대립의 천칭 (빌드 선호 속성)
- **준비**: **대립의 천칭**(`Charm_FireIce`) 1개
- **절차**: F8 → 오버레이 **선호속성 [불]** 클릭 → **왼쪽** 배치 추천 / **[얼음]** 클릭 → **오른쪽** 배치 추천
- 로그: 해당 아티팩트 `[위치→속성]`.

## T6. 신비 콤보 (×2 칸)
- **준비**: **MYSTIC** 태그 아티팩트 2개 이상 (예: 다용도 벨트, 용골 파편) → 신비 콤보 발동
- **기대**: 로그 `mysticCells>N` (>0), 오버레이에서 `★신비x2` 칸에 **레벨 높은 아티팩트** 배치.

## T7. 콤보(같은 태그 모으기)
- **준비**: 같은 태그 4~6개
  - PLANET: 날개 / 하늘색 행성 / 잿빛 행성 / 노란 행성
  - MAGITECH: 전격 차크람 / 라이트닝 볼트 / 썬더의 귀걸이 / 전격의 부적
- **기대**: 콤보 단계(2/4/6…) 만족하도록 활성 유지.

## T8. 행성 시너지
- **준비**: **행성 모듈**(`Charm_PlanetModule`, 로그 `[특수:PlanetNeighbor]`) + PLANET 아티팩트 여러 개
- **기대**: 행성 모듈을 행성들과 인접(8방향)하게 배치.

## T9. 마법서 시너지
- **준비**: **마법서**(`Charm_Magic`) 여러 개 + **인접 마법탄**(`[특수:NearMagicColumn]`) 또는 **마법 쿨감 보조**(`[특수:AdjacentMagic]`)
- **기대**: 인접 마법탄은 마법서가 많은 **열**에, 쿨감 보조는 마법서 **옆**에 배치.

## T10. 필러(포션)
- **준비**: 포션 가방이 없는 상태에서 **포션**(예: 재생의 포션) + 부적 여러 개
- **기대**: 포션이 로그/오버레이에서 **[필러]**, 석판·신비 칸을 피해 **구석/평범한 칸**에 배치.

## T11. 인벤토리 각인
- **준비**: 게임 기능으로 석판을 **인벤토리에 각인** (모드 아님)
- **기대**: F8 로그 `[base effects] derivedFromMatrix>0`, 각인 칸(+N)을 활용하는 추천. 적용 후 점수 유지.

---

## 빠른 회귀 세트 (릴리스 전 최소 점검)
1. **T1**(정의 석판) — 다중재시작·석판 조건
2. **T2**(회전) — 회전 자동
3. **T5**(대립의 천칭) — 선호 속성
4. **T10**(포션) — 필러
5. **T11**(각인) — 각인 인식

이 5개가 정상이면 릴리스 후보로 충분.
