# ───────────── Build artifacts ─────────────
bin/
obj/
*.user
*.suo

# ───────────── Copyrighted game files (never commit) ─────────────
# Assemblies copied from the game installation / decompiled sources
libs/
decompiled/

# ───────────── Third-party / downloaded binaries ─────────────
bepinex_dist/
BepInEx_win_x64_*/
BepInEx_win_x64_*.zip
dotnet-sdk-*.exe

# ───────────── Release artifacts (uploaded to Releases) ─────────────
# Track only README.md in dist; exclude everything else (ZIPs and temporary staging folders)
dist/*
!dist/README.md

# ───────────── Editor / OS ─────────────
.vs/
.idea/
.vscode/
Thumbs.db
.DS_Store

# Local Claude settings
.claude/
